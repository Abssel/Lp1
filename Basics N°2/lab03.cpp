#include <iostream>
using namespace std;
int area(int length, int width);
int main(){
int x4 = area(10,-7 ); // OK, pero rectangulo
// con ancho -7?
int x5 = a r e a ( 1 0 . 7 , 9 . 3 ) ; // Ok: pero llama a
// a r e a ( 1 0 , 9 )
char x6 = area ( 100, 9999 ); // Ok, pero trunca
// El resultado
return area(4,4);	
}
int  area(int lengt, int width);
{
	return length*width;
}
