#include <iostream>
using namespace std;
int area(int length, int width);
int main(){
	int x0 = arena(7); //Error, funcion no declarada
	int x1 = area(7); //Error, numero de argumentos
	int x2 = area("seven" ,2);//Error ;en tipo //del primer argumento
	return area(4,4);
}
int area(int length, int width);
{
	return length, int width;
}
